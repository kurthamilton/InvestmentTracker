javascript:(
function() {
    var bidPrices = document.querySelectorAll('[title="Bid Price"]');
    for (var bidPrice in bidPrices) {
        var cell = bidPrice.parentElement;
        var row = cell.parentElement;
        var
    }
})();